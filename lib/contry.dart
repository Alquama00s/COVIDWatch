import 'package:flutter/material.dart';

class Contry extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
          child: Text("tera country gaya"),
        );
  }
}
